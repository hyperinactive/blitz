struct FileSystem {}

impl FileSystem {
    pub fn read_file(path: &str) {
        todo!("read a file given a path")
    }

    pub fn writeFile(path: &str, data: &str) {
        todo!("writes to a file, data should be stringified")
    }
}
